# Local development profile
spring.application.name=capstone

# Local MySQL configuration (update these with your local DB details)
spring.datasource.url=jdbc:mysql://localhost:3306/startupsphere
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Magno_rayvaughan123
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server settings
server.port=8080
server.error.include-stacktrace=never

# JWT Security
security.jwt.secret-key=3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
security.jwt.expiration-time=3600000

# Session cookies (set secure=false for local HTTP testing)
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# SMTP Setup - Local testing with Mailtrap or similar
# Option 1: Mailtrap (recommended for testing)
spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=your_mailtrap_username
spring.mail.password=your_mailtrap_password
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Option 2: Console logging instead of sending emails (uncomment to use)
# spring.mail.host=localhost
# spring.mail.port=25
# spring.mail.username=
# spring.mail.password=
# spring.mail.properties.mail.smtp.auth=false

# SendGrid (commented out for local testing)
sendgrid.api.key=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
sendgrid.from.email=startupsphere332@gmail.com
sendgrid.from.name=StartupSphere

# Logging
logging.level.root=INFO
logging.level.com.yourpackage=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# Enable detailed Spring bean creation logging
logging.level.org.springframework.beans=DEBUG
logging.level.org.springframework.context=DEBUG

# CORS for local frontend testing
# spring.web.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# spring.web.cors.allow-credentials=true